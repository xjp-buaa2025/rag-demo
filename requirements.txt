# Core Infrastructure
python-dotenv
pyyaml

# Vector Database (Local File Mode, No Docker)
qdrant-client>=1.11   # Qdrant 本地文件模式（Windows 支持，替代 ChromaDB/Milvus）

# RAGFlow Deepdoc Dependencies
# deepdoc might require some external image/pdf handling libraries depending on your document types.
# This represents a baseline.
PyMuPDF
mineru[pipeline]
Pillow
docx2txt
openpyxl
pandas
beautifulsoup4
markdown
pdfplumber

# Embeddings (Local Model)
sentence-transformers
transformers
torch
# (optional for faster inference on CPU/GPU depending on torch setup)
torchvision  # Chinese-CLIP 图片处理需要
# torchaudio

# Multimodal: Chinese-CLIP（通过 transformers 加载）
# 模型权重：OFA-Sys/chinese-clip-vit-large-patch14（约 2GB，首次运行自动下载）

# LangChain Ecosystem
langchain
langchain-community
langchain-core
langchain-openai
# Note: For connecting to vLLM via open-ai compatible API
openai
tiktoken

# Graph Database (BOM → Neo4j)
neo4j

# Web/API integration for custom Skills in future
requests
aiohttp

# FastAPI 后端（重构自 app.py）
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
